{
  "name": "ts-workspaces",
  "workspaces": [
    "packages/**"
  ],
  "private": true,
  "scripts": {
    "build": "yarn workspace @tsw/foobar run build",
    "run": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
